<%- include('../../partials/layout', { title, body: `
<div class='grid lg:grid-cols-2 gap-6 items-center'>
  <div class='glass soft-shadow rounded-3xl p-8 fade-in'>
    <div class='flex items-center gap-4'>
      <img src='/public/img/logo.png' class='w-16 h-16 rounded-full soft-shadow floaty' alt='Logo'/>
      <div>
        <div class='text-2xl font-extrabold'>Registro</div>
        <div class='text-sm text-slate-600'>Crea tu cuenta para responder el cuestionario.</div>
      </div>
    </div>

    <form class='mt-6 space-y-4' method='POST' action='/auth/register'>
      <div>
        <label class='text-sm font-bold'>Nombre completo</label>
        <input class='input mt-1' type='text' name='full_name' placeholder='Tu nombre y apellido' required />
      </div>
      <div>
        <label class='text-sm font-bold'>Correo</label>
        <input class='input mt-1' type='email' name='email' placeholder='tucorreo@ucn.edu.ni' required />
      </div>
      <div>
        <label class='text-sm font-bold'>Contraseña</label>
        <input class='input mt-1' type='password' name='password' placeholder='Mínimo 6 caracteres' required />
      </div>
      <button class='btn btn-primary w-full' type='submit'>Crear cuenta</button>
    </form>

    <div class='mt-4 text-sm text-slate-600'>
      ¿Ya tienes cuenta? <a class='link' href='/auth/login'>Inicia sesión</a>
    </div>
  </div>

  <div class='glass soft-shadow rounded-3xl p-8 hidden lg:block fade-in'>
    <div class='text-xl font-extrabold'>¿Cómo funciona?</div>
    <ol class='mt-3 space-y-3 text-slate-600 text-sm'>
      <li><b>1.</b> Te registras con tu correo.</li>
      <li><b>2.</b> Lees el mensaje informativo y aceptas.</li>
      <li><b>3.</b> Respondes el cuestionario (toma pocos minutos).</li>
      <li><b>4.</b> Si se detecta un estado crítico, el equipo autorizado puede dar seguimiento por correo.</li>
    </ol>
    <div class='mt-6 card glass rounded-3xl'>
      <div class='font-bold'>Tip</div>
      <div class='text-sm text-slate-600 mt-1'>Responde con honestidad. No hay respuestas “buenas” o “malas”.</div>
    </div>
  </div>
</div>
` }) %>
